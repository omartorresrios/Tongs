<div class="overlay-main" style="background: url(<%= randomized_background_image %>) no-repeat center center fixed;z-index: 10000;">
  <%= image_tag('tongsession.svg', class: 'logo-main', height: 80, alt: "", title: "Tongs") %>
  <div class="welcome-desc">
    <p class="desc-first hidden-xs">Conoce los temas de los que hablan tus amigos y aprende de ellos.</p>
    <p class="desc-second"><strong>Comparte todo lo que sepas o te apasione.</strong></p>
    <p class="desc-third">Aumenta tu valor profesional ganando <strong>tongs</strong> 👅</p>
  </div>

  <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: { autocomplete: 'off' }) do |f| %>
    <div class="form-input-wrapper-sessions white-black">
      <%= f.text_field :login, autofocus: true, placeholder: "Nombre de usuario o correo", autocomplete: 'off', onfocus: "this.value = this.value;" %>
    </div>

    <div class="form-input-wrapper-sessions white-black">
      <%= f.password_field :password, autocomplete: "off", placeholder: "Contraseña" %>
    </div>

    <% if devise_mapping.rememberable? -%>
      <div class="field remember-me">
        <%= f.check_box :remember_me %>
        <%= f.label :Recuérdame %>
      </div>
    <% end -%>

    <%= f.submit "Inicia sesión", class: "button buttons-session" %>
    <!-- <p class="o">o</p>
    <%= link_to "Regístrate", new_user_registration_path, class: 'button a-to-button buttons-session pad-top' %> -->
  <% end %>

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { autocomplete: 'off' }) do |f| %>
    <%= devise_error_messages! %>

    <div class="form-input-wrapper-sessions white-black">
      <%= f.text_field :username, autofocus: true, placeholder: "Nombre Completo", onfocus: "this.value = this.value;" %>
    </div>

    <div class="form-input-wrapper-sessions white-black">
      <%= f.email_field :email, placeholder: "Correo" %>
    </div>

    <% if f.object.password_required? %>
      <div class="form-input-wrapper-sessions white-black">
        <%= f.password_field :password, autocomplete: "off", placeholder: "Contraseña (mínimo #{@minimum_password_length} caracteres)" %>
      </div>

      <div class="form-input-wrapper-sessions white-black">
        <%= f.password_field :password_confirmation, autocomplete: "off", placeholder: "Reingresa tu contraseña" %>
      </div>
    <% end %>

    <%= link_to "Olvidaste tu contraseña?", new_password_path('user'), :class => "login-link" %>

    <%= f.submit "Crear cuenta", class: "button buttons-session" %>
    <!-- <p class="o">o</p>
    <%= link_to "Inicia sesión", new_user_session_path, class: "button a-to-button buttons-session pad-top" %> -->

  <% end %>

  <ul class="auth">
    <li class="omniauth-button">
      <%= link_to user_omniauth_authorize_path(:facebook), class: 'button social facebook' do %>
        <%= icon('facebook-official') %>Entra con Facebook
      <% end %>
    </li>
    <!-- <li class="regular-signin">
      <%= link_to "Entra", new_user_session_path, class: "signin-signup-link" %> o
      <%= link_to "Regístrate", new_user_registration_path, class: "signin-signup-link" %> con tu correo
    </li> -->
  </ul>
  <!-- div class="footnote">Cañón de Waimea, Kauai</div-->
</div>