<% if post.tagged? %>
  <div class="feed-metadata">
    <%= icon('tag') %> <%= link_to post.tag.name, post.tag %>
  </div>
<% elsif post.recommended? %>
<div class="feed-metadata">
  <%= link_to post.recommender.username, post.recommender %> validó esto
  <% if (post.likes_count > 2) %>
    y <%= pluralize(post.likes.count - 1, 'otro') %>
  <% end %>
</div>
<% elsif post.featured? %>
  <div class="feed-metadata">
    Destacado por Gufi <%= pluralize(post.likes_count, 'otro') %>
  </div>
<% end %>